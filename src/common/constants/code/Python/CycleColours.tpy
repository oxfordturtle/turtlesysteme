# CycleColours

colinc: int = randint(0, 4)
colcode: int = randint(1, 20)
blank(black)
colour(rgb(colcode))
thickness(16)
length: int = 0
while not(length > 250):
  colcode = (colcode + colinc) % 20 + 1
  colour(rgb(colcode))
  length = length + 1
  forward(length)
  right(30)
