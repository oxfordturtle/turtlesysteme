# TurtleBounce

xvel: int = 3
yvel: int = -2
while (0 < 1):
  colour(white)
  blot(51)
  movexy(xvel, yvel)
  colour(red)
  blot(50)
  update()
  noupdate()
  if (turtx < 50) or (turtx > 950):
    xvel = -xvel
  if (turty < 50) or (turty > 950):
    yvel = -yvel
